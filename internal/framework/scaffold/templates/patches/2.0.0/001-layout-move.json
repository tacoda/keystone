{
  "description": "2.0 layout move — lift harness/ under .keystone/, lift lockfile to .keystone/lockfile.json, rename vendored policies dir. Idempotent. JSON-field renames inside keystone.json are NOT handled here — run `keystone migrate` for the schema bump.",
  "operations": [
    {
      "type": "move_dir",
      "path": "harness",
      "to": ".keystone/harness"
    },
    {
      "type": "move_file",
      "path": ".keystone/harness/keystone.lock.json",
      "to": ".keystone/lockfile.json"
    },
    {
      "type": "move_dir",
      "path": ".keystone/harness/plugins",
      "to": ".keystone/harness/policies"
    }
  ]
}
