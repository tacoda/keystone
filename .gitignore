/keystone
/keystone.exe
/dist/

# IDE / local tooling
/.serena/

# Keystone vendored policies — regenerated by `keystone install`.
.keystone/harness/policies/

# Web dashboard per-session audit log (created at runtime by `keystone web serve`).
.keystone/state/audit/
