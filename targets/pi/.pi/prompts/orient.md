---
description: Enter the planning phase — load idioms and state for the touched region
---

Enter the **planning** phase of the project harness.

Read `harness/guides/process/planning.md` and follow its activities.

Specifically for orient:

1. Identify the touched region(s) — directories the upcoming work will affect.
2. Read `harness/corpus/state/CODEBASE_STATE.md` for that region: which idioms apply, current coverage, active migrations.
3. Read `harness/corpus/idioms/<matching-stack>/*.md` for the loaded stacks.
4. Read `harness/corpus/state/migrations/active/*.md` for any migration affecting the touched region.
5. Surface what you found to the user before writing a plan.

Do not begin writing a plan until orient has surfaced what is already known.
